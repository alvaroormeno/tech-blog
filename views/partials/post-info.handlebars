

<article class="post">

    {{!-- <div class="title">
    
    <a href="{{description}}" target="_blank">{{title}}</a>
    <span>({{description}})</span>
  </div>
  <div class="meta">
    By {{user.username}} on {{format_date created_at}} | <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
  </div>
 --}}



  <div class="card" >

      <div class="card-header">
          <a href="{{description}}" target="_blank"><h3>{{title}}</h3></a>
      </div>
  
  <div class="card-body">
    
    <h5 class="card-title">
        <span>({{description}})</span>

    </h5>
    
  </div>
  <div class="card-body">
    By {{user.username}} on {{format_date created_at}} | <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
  </div>
</div>


</article>
    



    {{!-- <article class="post">
  <div class="title">
    <a href="{{post_url}}" target="_blank">{{title}}</a>
    <span>({{format_url post_url}})</span>
  </div>
  <div class="meta">
    {{vote_count}} {{format_plural "point" vote_count}} by {{user.username}} on {{format_date created_at}}
    |
    <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
  </div>
</article> --}}